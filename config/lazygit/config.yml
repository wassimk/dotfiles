git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never
gui:
  showIcons: true
  theme:
    selectedLineBgColor:
      - default
    selectedRangeBgColor:
      - default
keybinding:
  universal:
    scrollDownMain: <c-d>
    scrollUpMain: <c-u>
